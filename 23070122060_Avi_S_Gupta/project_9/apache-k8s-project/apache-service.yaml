apiVersion: v1
kind: Service
metadata:
  name: apache-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: apache-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
