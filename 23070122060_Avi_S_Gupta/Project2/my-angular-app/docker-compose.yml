# Development environment: Angular dev server with hot reload
services:
  angular-app:
    image: angular-app-dev:latest
    container_name: angular-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
