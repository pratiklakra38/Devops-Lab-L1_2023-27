apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: project8
  labels:
    app: event-service
spec:
  type: ClusterIP
  ports:
  - port: 31783
    targetPort: 31783
    protocol: TCP
  selector:
    app: event-service
