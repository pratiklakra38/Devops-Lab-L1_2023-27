apiVersion: v1
kind: Service
metadata:
  name: socialsphere-backend
  namespace: socialsphere
spec:
  selector:
    app: socialsphere-backend

  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001

  type: ClusterIP